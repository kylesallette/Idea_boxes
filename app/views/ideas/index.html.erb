<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <header>
    <img class=""src="" alt="banner" height="300px" width="100%">
  </header>
  <body>

<% @ideas.each do |idea| %>
<div class="ideas">
<h2><%= link_to idea.name, user_idea_path(@user, idea) %></h2>
<br>
<span class="button-wrap">
  <div class="button button-raised button-primary button-pill">
    <%= link_to "Delete",  user_idea_path(@user, idea), method: "delete" %>
  </div>
</span>
<span class="button-wrap">
  <div class="button button-raised button-primary button-pill">
    <%= link_to "Edit", edit_user_idea_path(@user, idea), method: "get" %>
  </div>
</span>

<br>
<% end %>
</div>

 </body>
</html>
